apiVersion: apps/v1
kind: Deployment
metadata:
  name: trend-store-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trend-store
  template:
    metadata:
      labels:
        app: trend-store
    spec:
      containers:
        - name: trend-store
          image: nandhini209/trend-store-app:latest
          ports:
            - containerPort: 80
